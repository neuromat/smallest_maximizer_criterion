# No string in the S is a suffix of any other string in the S
def check_properness(context_tree):
  context_tree.df
  import code; code.interact(local=dict(globals(), **locals()))

def check_completeness(context_tree):
  pass
